#include <iostream>
// #include "utils.h"
#include "types.h"
// #include "terminal_colors.h"
#include <cstring>

using namespace std;

int main() {
    Person *head;
    head->age = 12;
    cout << "??" << endl;
    // strcpy(head->name, "alisina");
    head->next = nullptr;
    cout << "??" << endl;
    // printAll(head);
    return 0;
}